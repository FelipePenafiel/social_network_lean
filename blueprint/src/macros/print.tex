% Macros used only by the printed version.
%
% The dummy definitions below make the pdf compiler ignore the macros that
% plasTeX provides and that make sense only for the web version.

\newcommand{\lean}[1]{}
\newcommand{\discussion}[1]{}
\newcommand{\leanok}{}
\newcommand{\mathlibok}{}
\newcommand{\notready}{}
% Make sure the arguments of \uses and \proves are real labels, by using
% invisible refs: LaTeX warns if a label is undefined, but nothing is printed.
\ExplSyntaxOn
\NewDocumentCommand{\uses}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\NewDocumentCommand{\proves}{m}
 {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}%
  \ignorespaces}
\ExplSyntaxOff
